# Optional — add your Anthropic API key to enable the AI assistant, weekly
# digest, and auto-extraction of text from PDFs and images.
# Without this, every other feature still works.
ANTHROPIC_API_KEY=

# Optional — where SQLite + uploaded files live. Defaults to ./data
# DATA_DIR=/absolute/path/to/data
